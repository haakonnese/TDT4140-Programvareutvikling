import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    width: "100%",
    display: "flex",
    minHeight: 200,
  },
  img: { height: "100%", maxHeight: 200 },
}));
